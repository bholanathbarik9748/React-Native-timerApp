import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: { flex: 1, padding: 16 },
    historyCard: { padding: 16, marginVertical: 8, backgroundColor: "#f5f5f5" },
});